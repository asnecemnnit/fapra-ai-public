{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "Launch Rendering",
          "type": "python",
          "request": "launch",
          "module": "rendering.renderingMain",
          "cwd": "${workspaceFolder}"
      }
  ]
}
